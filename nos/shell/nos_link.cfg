MEMORY {
    BOOT_SECTOR:  start = $0400,  size = $100,   type = ro, file = %O, fill = yes, fillval = $00;
    NOS_CODE:     start = $0500,  size = $300,   type = ro, file = %O, fill = yes, fillval = $00;
    OTHER_TRACKS: start = $0800,  size = $22000, type = ro, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    BOOT_SECTOR: load = BOOT_SECTOR, type = ro, start = $0400;
    CODE:        load = NOS_CODE,    type = ro, start = $0500;
}