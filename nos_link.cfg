MEMORY {
    INIT_SECTOR:   start = $8000,  size = $100, type = ro, file = %O, fill = yes, fillval = $00;
    BOOT_SECTORS:  start = $8100,  size = $F00, type = ro, file = %O, fill = yes, fillval = $00;
    OTHER_TRACKS:  start = $9000,  size = $22000, type = ro, file = %O, fill = yes, fillval = $00;
}

SEGMENTS {
    BOOT_INIT:     load = INIT_SECTOR, type = ro,  start = $8000;
    BOOT_CODE:     load = BOOT_SECTORS, type = ro,  start = $8100;
}